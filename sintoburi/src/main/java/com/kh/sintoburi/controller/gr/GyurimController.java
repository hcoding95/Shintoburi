package com.kh.sintoburi.controller.gr;

import org.springframework.stereotype.Controller;

@Controller
public class GyurimController {
	
	/*
	 * // 장바구니 테스트시 로그인 사용자 필요
	 * 
	 * @RequestMapping(value = "/gr", method = RequestMethod.GET) public String
	 * home(Model model, HttpSession session) { UserVo loginDto = new UserVo();
	 * loginDto.setUser_id("user01"); loginDto.setUser_pw("1234");
	 * loginDto.setUser_name("유저01"); loginDto.setGrade("구매자");
	 * session.setAttribute("login", loginDto);
	 * 
	 * return "redirect:/gr/basket/list"; }
	 */
	
}
