<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="org.zerock.ex01.mapper.EnquiryMapper">
 	  
 	  <select id="getList" resultType="EnquiryVo">
        select * from tbl_enquiry
		order by eno 
    </select>
    
    <insert id="insert">
    insert into tbl_enquiry
    (eno, user_id, enquiry_type, content, image)
    values
    (seq_enquiry.nextval, #{user_id}, #{enquiry_type}, #{content}, #{image})
</insert>
    
      <update id="update" >
        update tbl_enquiry
        set user_id = #{user_id},
            enquiry_type = #{enquiry_type},
            content = #{content},
            image = #{image}
        where eno = #{eno}
    </update>
    
       <delete id="delete" >
        delete from tbl_enquiry
        where eno = #{eno}
    </delete>
    
       <select id="selectByEno" resultType="EnquiryVo" >
        select * from tbl_enquiry
        where eno = #{eno}
    </select>
    
  
  </mapper>